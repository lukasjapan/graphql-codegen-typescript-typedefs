{
  "name": "graphql-codegen-typescript-typedefs",
  "version": "0.0.7",
  "description": "Adds the combined graphql schema (string) and type definitions (DocumentNode) to the generated typescript file.",
  "main": "index.js",
  "scripts": {
    "build": "tsc && cp README.md dist && cat package.json | jq 'del(.scripts,.dependencies)' > dist/package.json"
  },
  "author": "lukasjapan",
  "homepage": "https://github.com/lukasjapan/graphql-codegen-typescript-typedefs",
  "license": "MIT",
  "dependencies": {
    "@graphql-codegen/cli": "^5.0.3",
    "graphql": "^16.9.0"
  }
}
